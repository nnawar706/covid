import { MeetingCardProps } from '@/types'
import React from 'react'

const MeetingCard = ({ icon, title, date, isPrevious, 
    link, buttonIcon, buttonText, handleClick }: MeetingCardProps) => {
  return (
    <div>
      
    </div>
  )
}

export default MeetingCard
